name: tap_task
description: "Tap Tap Technologies Task"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  google_fonts: ^6.3.2
  go_router: ^17.0.0
  supabase_flutter: ^2.0.0
  dio: ^5.3.3
  skeletonizer: ^2.1.0+1
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
